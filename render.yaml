services:
  - type: web
    name: btc-regime
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: CACHE_TTL_SECONDS
        value: "3600"
      - key: DATA_DIR
        value: "/srv/data"
